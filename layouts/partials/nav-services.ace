.separator-btn
  h2.btn-title.btn.btn-outlined.btn-lg href="#"  Услуги
ul.row-flexbox.tabs-service.tabs-service-promo
  {{range $index, $element := where .Site.Pages "Section" "menu" }}
  a.tab href="{{.RelPermalink}}"
    .shadow-left
    .wrap
      .title.hypher {{.Title}}
      img.img-responsive src="{{.Params.icon}}"
    .shadow-right
  {{ end }}
